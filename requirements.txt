# requirements.txt                                                       
# Install all dependencies with: pip install -r requirements.txt                                                                                                                         
                                                                                                                                                                                           
flask==3.1.3          # Web framework — routing, templates, sessions                                                                                                                     
pandas==3.0.2         # CSV parsing and data manipulation for statement imports                                                                                                          
ofxparse==0.21        # OFX/QFX bank statement file parsing                                                                                                                              
python-dotenv==1.2.2  # Loads .env file into environment variables at startup
